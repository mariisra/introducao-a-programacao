#1) Faça um algoritmo para ler 20 números e armazenar em um vetor. Após a leitura total dos 20 números, o algoritmo deve escrever esses 20 números lidos na ordem inversa.
numeros = list(range(1, 21))
numeros.reverse()
print(numeros)

#2) Faça um algoritmo para ler 50 números e armazenar em um vetor VET, verificar e escrever se existem números repetidos no vetor VET e em que posições se encontram.
import random 

vet = []

for x in range(50):
  vet.append(random.randint(0, 100))
  print(vet)
  
print(vet)
  
for i in range(0, len(vet)):
   for k in range(0,len(vet)):
    if vet[i] ==  vet[k] and i != k:
      print(f'Índice: {k} | Valor: {vet[k]} \n')
